// Controls the reusable card container
import { Component } from '@angular/core'; // Imports Component from Angular

@Component({
  selector: 'app-card',        // The HTML tag used to show this component
  imports: [],                 // No other components are used here
  templateUrl: './card.html',  // Connects this component to its HTML file
  styleUrl: './card.css'       // Connects this component to its CSS file
})

export class Card {
}